enum AuthStatus {
  authenticated,
  unauthenticated,
}

enum NavigatePage {
  defaultPage,
  loginPage,
  homePage,
  forgotPasswordPage,
  resetPasswordPage,
}

enum StateStatus {
  initialState,
  loadingState,
  errorState,
  loadedState,
}

enum UserStatus {
  online,
  offline,
}
